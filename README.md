# Emacs Configuration

Clone this repository to `~/.emacs.d` directory and run Emacs.

Note: This configuration is only tested and adapted for TUI (terminal) mode, not GUI.

Compatible with Emacs 30.2.
